import React, { useState, useEffect } from 'react';

/**
 * Give/Set the user status.
 * @function
 * @param {string} id - Optional, use this when you want to verify other user status.
 */
const useActiveUser = (id) => {
    return useState
}

export default useActiveUser